import { default as CarouselContainer } from './carousel-container.svelte';
import { default as CarouselItem } from './carousel-item.svelte';

// Grouped carousel components
export const Carousel = {
  Container: CarouselContainer,
  Item: CarouselItem,
}